#!/usr/bin/env bash
# Wrapper to launch the Zephyrus TUI dashboard

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_DIR="$(dirname "$SCRIPT_DIR")"

if [ -f "${REPO_DIR}/.venv/bin/activate" ]; then
    source "${REPO_DIR}/.venv/bin/activate"
fi

exec python3 -m zeph_tui.app "$@"
